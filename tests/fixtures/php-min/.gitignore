# Generated by the verbs. Everything here is rebuilt by a CI run, and a fixture that
# commits its own output would be graded against a stale copy of itself.
/vendor/
/composer.lock
/coverage.xml
/infection.log
/semgrep.sarif
/.phpunit.cache/
/.phpunit.result.cache
/.phpstan.cache/
/.php-cs-fixer.cache
